[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
[project]
name = "task-api"
version = "1.0.0"
description = "Task management API for Jenkins CI/CD learning"
dependencies = [
    "Flask"
]
[tool.setuptools.packages.find]
where = ["src"]
